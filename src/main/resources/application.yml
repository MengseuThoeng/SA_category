spring:
  application:
    name: category
  #  security:
  #    user:
  #      name: admin
  #      password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5168/postgres
    driver-class-name: org.postgresql.Driver
